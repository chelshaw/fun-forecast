{{page-title 'Activity'}}
<Typography @level='h1'>When to
  {{this.model.verb}}
  in
  {{this.model.locationName}}</Typography>

<ForecastActivityDetail @hours={{this.model.hours}} @verb={{this.model.verb}} />
{{outlet}}