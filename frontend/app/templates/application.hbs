{{page-title 'Fun Forecast'}}
<div class={{if this.isDarkMode 'dark'}}>
  <div class='bg-white dark:bg-gray-800 dark:text-white'>
    <Navbar />
    <div class='mx-auto min-h-screen relative'>
      {{outlet}}
    </div>
  </div>
</div>